#include "cub3d.h"
#include "get_next_line.h"

char	**read_file(char *file_name)
{
	int		fd;
	char	**data;
	char	*line;

	if ((fd = open(path, O_RDONLY)) < 1)
		return (NULL);
	while ()
	close(fd);
	return (data);
}
